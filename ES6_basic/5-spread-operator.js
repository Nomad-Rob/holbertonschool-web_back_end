// This function concatenates two arrays and a string together.
export default function concatArrays(array1, array2, string) {
  // Use the concat method to concatenate array1, array2, and the string.
  // The result will be a new array containing the elements from both arrays and the characters of the string.
  return array1.concat(array2, string);
}
