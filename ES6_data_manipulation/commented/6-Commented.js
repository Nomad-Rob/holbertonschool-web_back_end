// This function, named 'setFromArray', takes an array ('arr') as input
// and returns a new Set containing the unique values from the array.
export default function setFromArray(arr) {
    // Create and return a new Set from the input array.
    return new Set(arr);
  }
  